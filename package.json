{
  "name": "staffone-portal",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.14.1"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node -e \"const fs = require('fs'); ['dist/server/wrangler.json', '.wrangler'].forEach(p => { if (fs.existsSync(p)) fs.rmSync(p, {recursive: true, force: true}) })\"",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.0.0",
    "astro": "^6.1.5",
    "lucide-static": "^0.460.0"
  }
}